name: "dino_ai_test"
version: "1.0.0"
config-version: 2

profile: "dino_ai_test"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dino_ai_test:
    staging:
      +materialized: view
    marts:
      +materialized: table
